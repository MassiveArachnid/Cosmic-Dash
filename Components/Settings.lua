






Settings = {

	BlockyRenderDistance = 32, -- Chunks are rendered as a collection of blocks
	ChunkyRenderDistance = 128, -- Chunks are rendered as a single, large block with the average color of its blocks
	DesiredFPS = 60, -- The desires fps the game will cap at. Used for computation limit timers.


}